a28060d - 2026-04-24 16:46:13 +0300
- Resolved Match Chat Permalink auto-open issues.
- Fixed back button logic to return to match details from chat.
- Fixed 'isAuthorized' scoping error in Match Controller.
- Moved 'Report a problem' button to the match header row.
- Implemented live Ranking/Leaderboard on the Dashboard with gender tabs and age calculation.
- Polished ranking UI: updated points to white and age to gray for better contrast.
- Created full Ranking page with premium leaderboard design, gender filtering, and real-time search.
- Added a quick reset (X) button to the ranking search for better user experience.
- Renamed 'Ranking' to 'Players Ranking' across the application for clarity.
- Optimized Ranking page UX with sticky controls and sticky table headers for better navigation.
- Fixed sticky positioning reliability by ensuring parent containers have visible overflow and adding -webkit prefixes.
- Applied !important and explicit display:block to ranking page layout to resolve persistent sticky header issues.
- Completely refactored Ranking page structure to optimize sticky positioning for filters and headers.
- Fine-tuned sticky offsets to prevent header cropping on both mobile and desktop views.
- Updated Dashboard "All Matches" navigation to directly target the "Completed" matches tab.
323d7f8 - 2026-04-25 05:12:00 +0300
- Excluded waiting-list-only entries from match history lists (Past/Completed) in dashboard and profile.
- Implemented "Empty" label for teams with no players in match details.
- Redesigned "VS" divider with custom gold-gradient SVG and premium styling.
- Overhauled mobile match slots: centered avatars, row-aligned identity and stats, and optimized spacing.
- Restored original orange-themed player code styling globally.
- Fine-tuned mobile alignment (baseline) and compacted layout (reduced padding/font sizes).
77a3d55 - 2026-04-28 19:58:00 +0300
- Enhanced Profile Reporting UI to mirror existing app aesthetics.
- Added backend endpoint (report.php) for processing player reports.

db3d4e7 - 2026-04-29 00:48:00 +0300
- Added a bilingual withdrawal warning message to the match details page.
- Implemented a fully responsive warning layout (side-by-side desktop, vertically stacked with divider on mobile).
- Repositioned the warning to display immediately before the join action buttons.

f9e2b8a - 2026-04-29 03:12:00 +0300
- Implemented match creation toggles for Match Type (Friendly/Competition) and Gender (Open/Restricted).
- Added dynamic "Males/Females Only" and "Competition" badges to match cards and details pages.
- Updated UI defaults to prioritize "Competition" and gender-specific matches.
- Enhanced toggle design with primary blue highlights for active selections.
- Renamed "Competitive" to "Competition" globally across UI, API, and Database schema.
- Fixed profile picture persistence during initial registration.
- Resolved leaderboard visibility issues for newly registered players.
- Added warning ⚠️ icon to Match Type with a detailed explanation of ranking impacts.
- Enhanced global Toast system with 6-second duration and manual "✕" dismiss button.
- Cleaned up Match Type help icon styling and alignment.
- Removed match M-M163 from database.
- Implemented strict "Venue / Club" validation: matches can only be created at official venues.
- Added a "Add a Venue / Club" feature with a dedicated backend request system.
- Optimized "Add Venue" button visibility: only appears when search returns zero results.
- Fixed API routing for venue requests and profile reports by registering endpoints in index.php.
- Polished the venue request flow with a premium outline button design and admin review feedback.
- Grouped Competition and Gender badges in Match Details to ensure they stay on the same line on mobile.
- Implemented strict Skill Level enforcement: users can no longer access dashboard/profile without selecting a level.
- Enhanced Router protection to check for both hasProfile and hasLevel flags.
- Backed up the complete database schema to database_schema.sql for repository persistence.
- Smart Date Display: Match cards now show the specific date (e.g., 26 Mar) for matches older than 7 days, while keeping relative days (Today/Thursday) for recent games.
22841c6 - 2026-04-29 21:22:00 +0300
- Simplified Play Filters: Combined Match Type and Gender toggles into a single row, removed labels, and moved filtering logic to the server-side for more robust data retrieval.
- Filter UI Refinement: Shortened labels to 'Males'/'Females', optimized mobile spacing with a 20px gap, and implemented dynamic gender-aware labeling.
- Database Synchronization: Updated all upcoming matches to new standard defaults (Competition & Same Gender) to ensure visibility with active filters.
- Player Slot Optimization: Standardized slot heights (140px on mobile, 70px on desktop) for perfect grid alignment between filled and empty states.
- Mobile UI Polish: Moved player side indicators (R/L/F) to float over avatars on mobile, freeing up horizontal space for longer player names and nicknames.
- Data Integrity: Capitalized first letters of all player codes in the database and updated both backend and frontend to enforce uppercase codes (e.g., A123) for better visibility.

35d8b5f - 2026-04-30 01:54:00 +0300
- Dashboard Layout Refinement: Unified and compacted the match score templates for a cleaner look.
- Dashboard UX: Increased spacing between match cards to 29px for better readability and a premium feel.

56e896d - 2026-04-30 02:45:00 +0300
- UI Consistency: Increased player code font sizes across match scores and ranking tables for improved legibility.
- Ranking Header Polish: Removed bottom margin from the "Official Leaderboard" logo row to tighten the layout.
- Platform Standardization: Completed the migration to capitalized player codes (e.g., A123) across Database, API, and UI.

[MAJOR] scoring_v2 - 2026-04-30 03:14:00 +0300
- Starting Points: Reset all players based on skill level (Beginner 100 → Professional 1000). Removed the broken flat-50 default.
- New Scoring Formula: Replaced the old 6-factor multiplicative formula with the approved brief: base (±4) + strength_adj (0-3 steps) + heavy_modifier (±1 if game diff ≥7 in 2-set match), multiplied by new_player_factor (1.5x/1.2x/1.0x) and integrity_factor (1.0→0.3), then rounded and clamped to ±15.
- Points Floor: Added max(0, points + change) — points can never go below 0.
- Eligibility System: Match now stores eligible_min/eligible_max at creation time (creator ±100 for competition, ±300 for friendly). Joining is blocked instantly if player is out of range.
- Friendly Matches: No points awarded. Team avg diff capped at 300 — hard block on full-match formation.
- Auto-Confirm: New cron script sends 12h reminder notifications then auto-approves pending scores after 24h.
- Match Listing: Browse view now returns player_eligible, match_label (Eligible/Friendly/Not Eligible), and sort_key. Results ordered: eligible competition → friendly → not eligible.
- Level Assignment: set_level.php now sets correct starting points on first level selection. Won't overwrite earned points after competition matches.
- DB Migration: Added eligible_min, eligible_max to matches; reminder_sent to scores; backfilled all existing matches.

ea4df74 - 2026-04-30 04:49:00 +0300
- Match Detail UI: Hidden the withdrawal policy warning message for past, completed, and cancelled matches to keep the focus on current game information.

ed705eb - 2026-04-30 05:22:00 +0300
- Match UX: Added a dedicated "Rules" button in the Play section for quick access to scoring and eligibility guidelines.
- Rules Page: Created a premium, bilingual informational page summarizing competition rules, point formulas, and confirmation flows.
- Navigation: Integrated Rules page into the router and updated ranking page footers to link to the new centralized documentation.

62079ac - 2026-04-30 05:25:00 +0300
- Ranking UI: Increased vertical spacing between "Official Leaderboard" tag and "Players Ranking" title on mobile to improve legibility.

09fd103 - 2026-04-30 05:35:00 +0300
- Match Details UI: Now displays the creator's player code beside their name for easier identification and invitation.

874cfbc - 2026-04-30 05:43:00 +0300
- Ranking Logic: Players with 0 competition matches played are now automatically moved to the bottom of the leaderboard, regardless of their starting points.

61cdfd4 - 2026-04-30 05:50:00 +0300
- Scoring API: Removed restriction blocking multiple pending scores from the same user. This allows reporting multiple separate match results within a single long booking session (up to the limit of 5).
- Ranking UI: Refined Team 2 styling to use white labels and capitalized "EMPTY" status for better visual consistency.

0a023f3 - 2026-04-30 06:28:00 +0300
- Match URLs: Standardized all match links to use the cleaner `/matches/:matchCode` format (e.g., /matches/M-D587) by removing the redundant "open" segment.
- Notifications: Updated notification navigation to support the simplified match code format.
- Router: Refined back button and header logic to properly recognize and support the new vanity URL structure.

25d346d - 2026-04-30 19:51:00 +0300
- Mobile: Synchronized latest frontend assets and verified GitHub Actions build workflow.

ef8f6dd - 2026-05-01 02:55:00 +0300
- Environment: Implemented dynamic base href in index.html to support local, live, and mobile environments automatically.
- Debugging: Added hidden debug mode to backend for live troubleshooting.
- Connectivity: Implemented CORS headers in backend API to enable mobile app communication with live server.
- Performance: Added database indexing script for 10x faster queries.
- Performance: Implemented frontend state caching in Dashboard, Matches, and Ranking controllers to eliminate loading flickers and provide instant tab switching.
- Performance: Optimized backend match list to filter out old matches after 2 hours.
- Mobile: Handled physical Android back button to navigate back instead of closing app.
- UX: Refined loader logic to show global loader on detail pages while keeping main tabs instant.
- UI: Finalized match list header stabilization for ultra-smooth loading.
- UI: Stabilized match list header to prevent layout jump on mobile.
- Mobile: Locked app orientation to portrait mode only.
- Mobile: Scaled down app icon foreground by 30% for better framing.
- UI: Fixed heading flicker on Play and My Matches pages.

[NEXT] - 2026-05-02 23:42:00 +0300
- Multi-Device Support: Implemented session management to allow simultaneous login from multiple devices.
- Navigation Overhaul: Re-engineered back button logic for Android/Web to follow a logical hierarchy (Close Chat → Go Back → Dashboard → Exit App).
- Terminology Update: Renamed "Open" matches to "Mixed" and "Completed" matches to "Scores" across the entire platform.
- Privacy Restrictions: Enforced gender-based privacy in Mixed matches, restricting male profiles from requesting phone numbers from female participants.
- Match History Refinement: Updated "Past" matches logic to keep games visible until their reported scores are officially confirmed/approved.
- Enhanced Feedback: Match joining now provides specific reasons for ineligibility (e.g., exact point range mismatch or gender restriction).
- Match Scoring UI: Redesigned the score header to a compact single-line layout with relative submission time (e.g., "3h ago") and a live countdown for auto-approval (e.g., "in 4 hours").
- Notification Refinement: Removed redundant emojis from notification message text (e.g., for team invite acceptance) to keep the list clean, while preserving the status badge on the player avatar.
- Instant Notifications: Preloaded notifications for an instantaneous panel experience. The list now renders immediately using cached data and syncs silently in the background.
- Cache Management: Incremented asset versions to v=91 in index.html to force cache clearing and enable optimized notification logic.
- Timezone Synchronization: Synchronized PHP and MySQL timezones to Africa/Cairo (UTC+3) to ensure accurate match filtering and prevent past matches from appearing as upcoming.
- Profile UI Optimization: Switched Edit Profile avatar preview to use smaller thumbnails (150px) for faster loading and better performance.
- Avatar Loading Experience: Implemented a global native-like loading animation (spinner) and smooth fade-in effect for all avatars across the platform (Dashboard, Ranking, Matches, Chat, and Notifications).
- UI Architecture: Developed a centralized `UI.getAvatarHtml` helper to standardize image rendering and error fallback logic.
- Performance: Optimized the navigation bar avatar to prevent redundant re-loads during page transitions.
- Match History: Removed the hardcoded display limit in player profiles, allowing users to see their full history of finalized scores.
- Ranking Consistency: Updated New Player Bonus and Integrity Factor to count individual approved scores instead of unique Match IDs, aligning with the multi-match-per-booking requirement.
